# Frontend – IoT Asset Tracking Web Interface

This is the frontend for the IoT Asset Tracking and Sensor Monitoring system.

It is a lightweight static web application built using HTML, CSS and JavaScript.  
The frontend communicates with the deployed ASP.NET Core Web API backend to perform full CRUD operations.

It communicates with the backend using the Fetch API:

```javascript
const API = "https://iotassettracking-webapp-backend-bnfjf5gpbdbhcpb5.southafricanorth-01.azurewebsites.net/api";




